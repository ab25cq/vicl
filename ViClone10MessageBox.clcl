include "ViClone9SearchMode.clcl"

class ViClone
{
    def messageBox(str:String) {
        clear();
        mvprintw(0, 0, str);
        refresh();

        getch();
        
        clear();
        view(); 
    }
}
